<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.net.mall.inventory.mapper.InventoryLogMapper">

    <insert id="insert" parameterType="cn.net.mall.inventory.entity.InventoryLogEntity">
        INSERT INTO inventory_log (id, product_id, batch_id, type, quantity,
            before_val, after_val, order_id, remark, create_time)
        VALUES (#{id}, #{productId}, #{batchId}, #{type}, #{quantity},
            #{beforeVal}, #{afterVal}, #{orderId}, #{remark}, NOW(3))
    </insert>
</mapper>
