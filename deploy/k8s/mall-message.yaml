apiVersion: apps/v1
kind: Deployment
metadata:
  name: mall-message
  namespace: mall
  labels:
    app: mall-message
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mall-message
  template:
    metadata:
      labels:
        app: mall-message
    spec:
      containers:
        - name: message
          image: mall-message:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: TZ
              value: Asia/Shanghai
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: mall-message
  namespace: mall
spec:
  selector:
    app: mall-message
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
